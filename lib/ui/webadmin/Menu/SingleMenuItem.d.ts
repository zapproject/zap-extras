import * as React from 'react';
import { ViewItem } from "./Menu";
export declare const SingleMenuItem: React.MemoExoticComponent<({ item, active }: {
    item: ViewItem;
    active: boolean;
}) => JSX.Element>;
